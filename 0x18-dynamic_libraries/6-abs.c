#include "main.h"
/**
 * _abs - computes the abs value of an integar
 * @n: the integar in question
 *
 * Return: the abs of the int.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
